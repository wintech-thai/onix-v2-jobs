<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
    th { background-color: #f5f5f5; }
  </style>
</head>
<body>
  <h2>Status Report</h2>
  <table>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
    <% @statusItems.each do |item| %>
      <tr class="status-<%= item['status'] %>">
        <td><%= item['name'] %></td>
        <td><%= item['description'] %></td>
      </tr>
    <% end %>
  </table>
</body>
</html>
